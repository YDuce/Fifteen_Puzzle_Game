<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight"> <!-- Ripple color -->

    <!-- Background shape -->
    <item>
        <layer-list >

            <!-- Background shadow -->
            <item>
                <shape android:shape="rectangle">
                    <solid android:color="#CCCCCC" /> <!-- Light gray shadow color -->
                    <corners android:radius="8dp" />
                    <padding
                        android:bottom="2dp"
                        android:left="2dp"
                        android:right="2dp"
                        android:top="2dp" />
                </shape>
            </item>

            <!-- Main button shape with gradient and border -->
            <item
                android:bottom="2dp"
                android:left="2dp"
                android:right="2dp"
                android:top="2dp">
                <shape android:shape="rectangle">
                    <solid android:color="@android:color/white" /> <!-- Main button color -->
                    <stroke
                        android:width="2dp"
                        android:color="#888888" /> <!-- Border color -->
                    <corners android:radius="8dp" />
                    <gradient
                        android:angle="90"
                        android:endColor="#E0E0E0"
                        android:startColor="#FFFFFF" /> <!-- Gradient effect -->
                </shape>
            </item>
        </layer-list>
    </item>
</ripple>
