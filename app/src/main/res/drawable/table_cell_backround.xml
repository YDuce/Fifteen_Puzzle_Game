<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Solid background color -->
    <solid android:color="@color/md_theme_background" />

    <!-- Define a thinner border color and thickness -->
    <stroke
        android:width="1dp"
        android:color="@color/md_theme_secondaryContainer" />

    <!-- Remove corner radius for a sharp, rectangular border -->
    <corners android:radius="0dp" />
</shape>
